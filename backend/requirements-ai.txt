-r requirements.txt
ultralytics>=8.3,<9
easyocr>=1.7,<2
# Install the appropriate PyTorch build FIRST, especially on Jetson.
# ONNX: install onnxruntime (CPU) or a compatible onnxruntime-gpu build.
# TensorRT: install through the target JetPack; export engines on that target.
